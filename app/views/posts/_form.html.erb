
<%= form_with model: @post, local: true do |f| %>
<div class=form-group>
  <span><i class="fas fa-edit"></i> <%= f.label :content %></span>
  <%= f.text_field :content, class: 'form-control' %>
</div>
<%= f.submit '投稿', class: 'btn btn-primary' %>
<% end %>
